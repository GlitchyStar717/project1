import 'package:flutter/material.dart';

class PhoneRecommendation extends StatelessWidget {
  const PhoneRecommendation({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Recommendation Page');
  }
}
